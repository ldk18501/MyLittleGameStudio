# Command Alias: concept

`concept` is a legacy alias.

- If the owner wants ideation, references, pitch, or a concept package, route to `/mlgs-brainstorm`.
- If the concept already exists and the owner wants systems or tasks, route to `/mlgs-plan`.

Record trace using the routed command.

